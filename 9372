#include <iostream>
#include <vector>
using namespace std;
int a,b,t,n,m;
int main(){
    cin >> t;
    for(int i=0;i<t;i++){
        cin >> n >> m;
        for(int j=0;j<m;j++) cin >> a >> b;
        cout << n-1 << endl;
    }
}
